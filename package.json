{
  "name": "@mousepox/static-server",
  "version": "0.1.0",
  "description": "A dead simple, zero-dependency static file server for Node.js.",
  "keywords": [
    "static",
    "server"
  ],
  "author": "Geoff Blair <geoff@mousepox.io>",
  "license": "ISC",
  "repository": "github:geoffb/mousepox-static-server",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepublish": "yarn build",
    "test": "tape 'tests/**/*.js'"
  },
  "devDependencies": {
    "@types/node": "^12.7.2",
    "got": "^9.6.0",
    "tape": "^4.11.0",
    "typescript": "^3.5.3"
  },
  "publishConfig": {
    "access": "public"
  }
}
